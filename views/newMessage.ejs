<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Post New Message - ScaxCodes mini message board</title>
    <!-- I am aware that this is not the best way to include tailwindcss
    but I am using it for the sake of simplicity in my first fullstack project -->
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
      <h1 class="text-3xl font-bold mb-6">Post a New Message</h1>
      <form method="POST" action="/new" class="space-y-4">
        <div>
          <label for="user" class="block text-gray-700 font-medium mb-1">
            Your Name
          </label>
          <input
            type="text"
            id="user"
            name="user"
            class="w-full p-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
        </div>
        <div>
          <label for="text" class="block text-gray-700 font-medium mb-1">
            Your Message
          </label>
          <textarea
            id="text"
            name="text"
            rows="4"
            class="w-full p-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          ></textarea>
        </div>
        <div class="flex space-x-4">
          <button
            type="submit"
            class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600"
          >
            Submit
          </button>
          <a
            href="/"
            class="bg-gray-400 text-white px-4 py-2 rounded hover:bg-gray-500"
          >
            Cancel
          </a>
        </div>
      </form>
    </div>
    <!-- Loading Spinner Modal -->
    <div
      id="loadingModal"
      class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50 backdrop-blur-sm hidden"
    >
      <div
        class="w-16 h-16 border-4 border-t-4 border-gray-200 rounded-full animate-spin border-t-blue-500"
      ></div>
    </div>
    <script>
      // Show the loading spinner modal when the form is submitted.
      document.querySelector("form").addEventListener("submit", function () {
        document.getElementById("loadingModal").classList.remove("hidden");
      });
    </script>
  </body>
</html>
